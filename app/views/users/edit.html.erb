<% provide(:title, "Edit Profile") %>

<h1>Edit Profile</h1>
<%= form_for @user,  html: { multipart: true } do |f| %>
  <%= f.file_field :avatar %>

  <%= f.text_field :name %>

  <%= f.submit "Edit" %>
<% end %>
