<%= form_for comment, remote: true do |f| %>
  <%= f.hidden_field :review_id, value: review.id %>
  <%= f.label :comment %>
  <%= f.text_field :content, class: "form-control", id: "comment-field-#{review.id}" %>
<% end %>
